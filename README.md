# PythonSMTP-Email-Sender
Python Code to send an email using email and smtplib

Edit the ".env.example" file and rename to "SMTP.env" or change where dotenv calls the file name to load the environment variable to add email and password to send from.

The HTML template is simple and modifiable to fit your needs. This is just an example HTML template.
